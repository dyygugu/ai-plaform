# aidp-monitor-next TODO

更新时间：2026-05-05

## 执行约束

- 自主执行：除重大架构变更、真实线上切换、破坏性操作外，默认继续推进。
- 删除规则：任何删除候选只允许移动到 `D:\数据标注插件\delete`，不直接删除。
- 正式域名：`manage.51gugu.uk` 当前被 gugunas Docker 老版本使用，新版本完成测试前不切换反代。
- 验收目标：代码树清晰、模块边界明确、自动验证通过后再交给人工验收，尽量一次通过。

## P0 已完成

- [x] 新建独立工程 `Projects/aidp-monitor-next`。
- [x] FastAPI + Pydantic Settings + SQLAlchemy/Alembic 骨架。
- [x] `/api/v1/health` 健康接口。
- [x] React + TypeScript + Vite + Ant Design + React Router 骨架。
- [x] Docker Compose 开发编排：PostgreSQL、Redis、API。
- [x] README、`.env.example`、基础验证。

## P1 核心数据与任务看板

- [x] 建立核心 SQLAlchemy 模型：账号、任务目录、审计。
- [x] 建立 Alembic 初始表迁移。
- [x] 实现任务页来源账号配置读取接口；更新接口待权限模块后开放。
- [x] 实现任务目录 API：列表、样本占位、状态颜色、待处理原样展示。
- [x] 实现主账号任务页只读脱敏样本采集服务骨架。
- [x] 前端首页和任务看板接 API 数据，保留接口不可用时占位兜底。
- [x] 加入状态颜色标签和简称规则；任务详情页字段骨架待下一步。

## P2 账号、权限与审计

- [x] 账号管理 API：列表、健康状态、登录态摘要、失败原因。
- [x] 权限矩阵：管理员、操作员、只读。
- [x] 审计日志模型/API：已完成基础查询与写入服务，事件覆盖继续扩展。
- [x] 前端账号管理、权限审计页面接入真实 API。

## P3 备份、告警与恢复演练

- [x] 备份配置：本机 7 天、gugunas 30 天、每天 03:30 清理。
- [x] 备份任务 API：连接测试、手动备份、备份列表已完成；清理执行待接定时器。
- [x] 飞书告警模板：标题、严重级别、账号/任务、原因、最近时间、trace_id、面板链接。
- [x] 恢复演练验收项：登录、账号配置、任务目录、看板三列、AI mock、worker 心跳、备份记录、审计查询。

## P4 AI、Worker 与统计

- [x] AI mock 队列 API 与前端展示。
- [x] Worker 心跳模型/API 与前端展示。
- [x] 收益三项原名 + 可配置显示名 + 今日收益 + 小时收益。
- [x] Excel 报表导出骨架。

## P5 集成与验收

- [x] 后端规则 smoke 与 AST 验证；完整单元/契约测试继续补。
- [x] 前端类型检查和构建。
- [x] Docker Compose config smoke。
- [x] 主账号任务页只读脱敏样本采集验证。
- [x] 自动验收报告生成。
- [x] 人工验收清单。
- [x] 本地 Docker 重新构建部署到 `http://127.0.0.1:8789`，确认网页端可见新版程序。

## P6 任务详情与来源配置

- [x] 任务详情 API：展示状态历史、待处理历史、来源账号、覆盖账号数、最近失败、事件时间线。
- [x] 任务页来源账号可编辑：默认主账号，修改写审计，并提示触发重采集。
- [x] 任务页手动刷新：基于主账号只读采集生成脱敏样本，并将任务目录写入数据库。
- [x] 简称规则维护：内置常见前缀，提供后台可编辑前缀和单任务手动简称覆盖骨架。
- [x] 前端任务看板：详情抽屉、来源配置表单、刷新按钮、stale/error 状态展示。
- [x] P6 自动验收：API smoke、前端构建、Docker smoke 覆盖详情和刷新入口。
- [x] 待处理数字口径修正：从任务页当前可处理节点 `OperatorStat.ToDo`/`NodeStat.ToDo` 取数字，不再显示节点名称占位，也不取全节点最大值。

## P7 规则中心与 Worker 深化

- [x] 规则中心页面：版本列表、版本对比、发布、灰度、回滚、命中率骨架。
- [x] 规则后端 API：规则版本、发布事件、命中统计、审计记录。
- [x] Worker 管理深化：绑定账号、版本更新、日志摘要、任务领取/事件上报骨架。
- [x] 首页指标增强：账号健康、今日收益、AI 队列、worker 在线数、告警摘要聚合。


## P8 运行编排与发布护栏

- [x] 系统任务编排 API：任务定义、手动运行、运行历史、失败审计。
- [x] 备份清理执行：按本机保留天数移动过期备份到隔离目录，不直接删除。
- [x] 发布门禁 API：聚合健康、任务样本、规则版本、Worker、备份、演练、审计状态，明确正式域名仍需人工切换。
- [x] 前端运维中枢：任务编排、备份清理、发布门禁和运行历史可视化。
- [x] P8 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P9 生产护栏与真实调度深化

- [x] 调度计划 API：展示任务间隔、最近运行、下次运行、是否到期。
- [x] 调度 Tick API：一次性执行到期任务，支持 dry-run、limit、审计和运行历史。
- [x] 正式域名切换 Runbook API：生成手动切换步骤、验证项、回滚项和风险提示，不自动切换。
- [x] 前端生产护栏页：调度计划、Tick 执行、域名切换 Runbook、门禁摘要可视化。
- [x] P9 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P10 生产观测与采集守护

- [x] 观测摘要 API：汇总数据库、任务目录、账号、Worker、AI、备份、审计、调度状态。
- [x] 采集守护 API：展示只读采集样本新鲜度、来源账号、任务目录 stale/error 和安全模式。
- [x] 事件时间线 API：聚合审计、运维任务、Worker 事件、备份、规则发布事件。
- [x] 探针运行 API：执行健康、数据库、任务目录、规则、Worker、发布门禁探针并写审计。
- [x] 前端观测中心：摘要卡、采集守护、探针结果、事件时间线可视化。
- [x] P10 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。

## P11 告警闭环与 SLO 演练

- [x] 告警规则 API：展示内置规则、严重级别、SLO 目标、触发来源和静默策略。
- [x] 告警评估 API：基于观测摘要、发布门禁、采集守护、账号健康生成本地告警事件。
- [x] SLO 摘要 API：展示可用性、采集新鲜度、Worker 在线、发布门禁和备份成功率。
- [x] 演练通知预览：生成飞书格式但不发送，写审计作为闭环证据。
- [x] 前端告警中心：规则、SLO、评估结果、演练通知和闭环证据可视化。
- [x] P11 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P12 交付验收中心与证据包

- [x] 交付摘要 API：汇总最新验收报告、截图、TODO 状态、Docker 入口和正式域名手动切换状态。
- [x] 证据包 API：生成 Markdown 交付证据包，收集自动验证、截图、关键接口和不变更项。
- [x] 验收清单 API：输出人工验收步骤、必看页面、风险提示和回滚提醒。
- [x] 前端交付中心：展示交付摘要、证据包、验收清单、截图索引和正式域名手动提醒。
- [x] P12 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P13 日常巡检与运行基线

- [x] 巡检摘要 API：汇总健康、任务目录、采集守护、发布门禁、SLO、交付证据和正式域名状态。
- [x] 巡检清单 API：输出日常巡检项、证据路径、状态、处理建议和下一步动作。
- [x] 巡检运行 API：生成本地 Markdown 巡检记录，写审计作为每日巡检闭环证据。
- [x] 前端巡检中心：展示巡检摘要、检查项、下一步动作、风险提示和巡检记录。
- [x] P13 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P14 手动切换前冻结基线

- [x] 冻结摘要 API：汇总健康、交付、巡检、发布门禁、域名 Runbook 和冻结状态。
- [x] 冻结清单 API：输出切换前冻结项、回滚项、证据路径和人工确认项。
- [x] 冻结清单生成 API：生成本地 Markdown 冻结清单，写审计作为切换前基线证据。
- [x] 前端冻结中心：展示冻结摘要、冻结项、回滚项、证据路径和手动切换提醒。
- [x] P14 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。


## P15 8787 账号迁移与登录态基线

- [x] 旧版 8787 账号预检 API：读取 `accounts.docker-local.json`，只返回脱敏账号元数据和迁移规则。
- [x] 账号迁移执行 API：将 7 个旧版账号按当前规则导入新库，主账号保持任务来源，不复制 Cookie 明文。
- [x] 迁移证据包：生成本地 Markdown 账号迁移记录，写审计 trace，标记需要人工重新登录的账号。
- [x] 前端账号管理页：展示旧版账号预检、迁移结果、状态基线、证据路径和 Cookie 不落库提醒。
- [x] P15 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖，确认 8789 已有 7 个账号。

## 大纲封顶计划

- 固定终点：P20。
- 当前进度：已完成到 P20。
- 剩余阶段：P0-P20 已全部完成，待用户人工验收与手动域名切换。
- 约束：除非用户新增真实业务方向或重大架构要求，不再自动追加 P21+。
- 域名提醒：按用户要求，等 P20 全部完成并通过最终验收后再提醒手动切换正式域名。

## P16 多账号任务覆盖与登录态复核

- [x] 7 个迁移账号的任务可见性基线：账号维度任务数量、任务来源、异常账号和证据路径。
- [x] 登录态复核 API：基于现有安全规则输出 active/stale/needs_login 判断，不复制 Cookie 明文。
- [x] 前端账号任务覆盖页：展示 7 账号覆盖矩阵、主账号来源、异常提示和处理建议。
- [x] P16 自动验收：API smoke、前端构建、Docker smoke 与截图覆盖。

## P17 数据正确性与收益口径封版

- [x] 收益三项、今日收益、小时收益与账号/任务维度口径封版。
- [x] 报表导出增强：覆盖 7 账号、任务简称、待处理数字、时间戳和证据路径。
- [x] 数据一致性检查 API：任务目录、收益、账号、Worker 与审计之间的数量校验。
- [x] P17 自动验收：导出文件、API smoke、前端构建、Docker smoke 与截图覆盖。

## P18 异常处置与运维闭环封版

- [x] 账号异常、采集异常、Worker 异常、备份异常的处置 Runbook 汇总。
- [x] 本地闭环 API：生成异常处置记录、审计 trace 和下一步建议，不触发外部系统。
- [x] 前端异常处置中心：展示异常队列、处置建议、证据路径和闭环记录。
- [x] P18 自动验收：异常演练、API smoke、前端构建、Docker smoke 与截图覆盖。

## P19 最终验收矩阵与回滚演练

- [x] 最终验收矩阵：页面、接口、Docker、账号、任务、收益、告警、巡检、冻结逐项通过。
- [x] 回滚演练证据：旧版 8787 保留、新版 8789 运行、正式域名未切换、回滚步骤可执行。
- [x] 最终证据包：汇总 P7-P19 截图、报告、迁移记录、巡检记录、冻结记录和风险项。
- [x] P19 自动验收：最终矩阵生成、Docker smoke、证据包和截图覆盖。

## P20 大纲封版与切换提醒

- [x] 大纲封版 API：确认 P0-P20 TODO 全部完成、最新 Docker smoke passed、关键证据存在。
- [x] 封版页面：展示最终状态、剩余人工动作、域名切换条件和回滚提醒。
- [x] 最终报告：生成大纲封版报告，明确系统不自动切换正式域名。
- [x] P20 自动验收：封版报告、API smoke、前端构建、Docker smoke 与截图覆盖。
- [x] 完成后再提醒用户：可以人工切换 `manage.51gugu.uk` 反代，并保留旧版回滚方案。

## 生产化补漏（不追加新阶段）

- [x] 差距审计：对照旧选项记忆，确认待处理实时刷新、新账号登录入口和生产文案缺口。
- [x] 任务看板：补“最近摘要/实时只读 SearchTask”手动切换、刷新待处理数据按钮、15 分钟自动刷新开关、刷新证据与错误提示。
- [x] 账号管理：补新账号待登录占位、重新登录入口、本机登录助手命令、打开登录窗口、同步登录态与 `/api/client-session` 兼容接口。
- [x] 生产化 UI：主导航去阶段化，证据页面降级为“运维证据”分组，前端页面移除 P 数、大纲封版、最终验收等生产不应暴露文案。
- [x] 回归验证：API smoke、前端构建、完整 Docker 重部署、Docker smoke、截图和 UTF-8 无 BOM 检查。
